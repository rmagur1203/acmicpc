#include <iostream>

using namespace std;

int arr[]

int init(int start, int end, int node){
    if (start == end) return tree[node] = 
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;

}