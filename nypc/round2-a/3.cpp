#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <tuple>
#include <unordered_map>

using namespace std;

int score[1000][1000];

void scoring(int x, int y){
    if (y + 1 >= N) return;
    if (x - 1 > 0) map[y][x - 1] += 1;
}

int main(){
    cin.tie(NULL)->sync_with_stdio(false);

    
}